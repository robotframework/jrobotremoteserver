# provides a way to access Maven properties during the test phase. Resource filtering will replace the right-hand side with values during the build process.
# There is another way of doing this, but it involves having an element in the POM for every property.
project.name=${project.name}
project.version=${project.version}
testLibraryClass=${testLibraryClass}